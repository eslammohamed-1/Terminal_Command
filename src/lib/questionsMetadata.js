export function getMcqCountForDay(dayId) {
  // Every day in the curriculum has exactly 30 MCQ questions
  return 30;
}

export function getTypingCountForDay(dayId) {
  // Every day in the curriculum has exactly 30 typing exercises
  return 30;
}
